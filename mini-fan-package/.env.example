# 与 Web 项目相同的 Supabase 项目（anon key，勿提交 service_role）
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# AI 代理 BFF（不要末尾 /）
# 启动 BFF：须在仓库根目录的兄弟目录 bff-server 里执行 npm start；若在 mini-fan-package 目录下可：npm run bff:start
#
# 微信开发者工具模拟器可用 http://localhost:端口；真机预览/真机调试时，手机上的 localhost 是手机自己，
# 且 localhost 不能设为「request 合法域名」。请改为电脑的局域网 IP，例如（Mac 可查：ipconfig getifaddr en0）：
#   VITE_API_BASE_URL=http://192.168.0.108:8787
# 端口须与 bff-server 的 PORT 一致（默认 8787；若 .env 里改成 8800 则这里也写 8800）
# 手机与电脑同一 Wi‑Fi；开发者工具「详情 → 本地设置」勾选「不校验合法域名、web-view、TLS 版本以及 HTTPS 证书」。
# BFF 仅需本机可访问时，Node 默认监听 0.0.0.0，手机才能连上电脑的端口。
#
# 可选：首页「今日菜单」Banner 左上角城市+天气（未实现或失败时用本地 MOCK）
# GET /api/miniapp/home-banner-ambient
# 示例：{ "city_name":"深圳","weather_text":"晴","weather_icon_emoji":"☀️" } 或带 weather_code：sunny/cloudy/rain…
#
# 需实现 GET /api/miniapp/config 返回小程序文案（可与 AI 共用同一域名）
# 响应体支持扁平 snake_case / camelCase / { data|config|miniapp } 包装 / home·plaza·profile 分组；
# 首页运营位：home_banner_*、show_home_banner、home_recommend_*、show_home_recommend、home_hot_*、show_home_hot（及分组 home_banner / home_recommend / home_hot），详见 src/api/configRemoteMap.ts
# 功能广场：plaza_entries 数组（或 plaza.entries），元素字段 key、title、subtitle、icon、route、enabled、sort_order；可与 id/path/sortOrder 等别名映射，见 src/lib/plazaEntriesMerge.ts
# 收藏/历史页：favorites_*、histories_*、show_recent_favorites、show_recent_histories；或分组对象 favorites / histories（含 empty_title、empty_button_text、show_recent 等），见 src/api/configRemoteMap.ts
# 全局文案 / Toast / 未登录空态：login_prompt_*、login_button_text、toast_*、common_empty_*；或分组 login / login_prompt、toasts、common_empty，见 src/api/configRemoteMap.ts
VITE_API_BASE_URL=https://your-bff.example.com

# 可选：静态 JSON 兜底（GET）；与 BFF 同时配置时以 BFF 为准；域名需加入微信 request 合法域名
# VITE_APP_CONFIG_URL=https://your-cdn.example.com/miniapp-config.json
